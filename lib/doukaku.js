class Block {
  constructor(height) {
    this.height = height
    this.water = 0
    this.isEdge = false
  }
}

const main = (...argv) => {
  const input = argv[0]
  return input
}

module.exports = {
  main,
  Block,
}
